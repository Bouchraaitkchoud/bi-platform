import RegisterPage from '@/features/auth/pages/RegisterPage';

export const metadata = {
  title: 'Register - BI Platform',
  description: 'Create a new BI Platform account',
};

export default function Page() {
  return <RegisterPage />;
}
